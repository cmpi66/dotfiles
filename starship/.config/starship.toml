# Get editor completions based on the config schema
"$schema" = 'https://starship.rs/config-schema.json'

format = """$all"""

# move the rest of the prompt to the right
# right_format = """${custom.pending} ${custom.urgent} ${custom.low} $time"""
right_format = """${custom.due} $time"""

# Wait 10 milliseconds for starship to check files under the current directory.
scan_timeout = 10

# Wait 700 milliseconds for starship command  to check run this way the task command won't time out
command_timeout = 1000

[custom.pending]
# command = "task count rc.gc=off rc.verbose=nothing status:pending"
# # command = "task count rc.gc=off rc.verbose=nothing status:pending && task count rc.gc=off rc.verbose=nothing priority:H"
# when = "which task"
# symbol = " "
# format = "[$symbol$output]($style)"
# description = "Count of pending Taskwarrior tasks"

[custom.due]
command = "task count rc.gc=off rc.verbose=nothing due:monday status:pending"
when = "which task"
symbol = " "
style = "bold red"
format = "[$symbol$output]($style)"
description = "Count of pending Taskwarrior tasks"

# [custom.urgent]
# command = "task count rc.gc=off rc.verbose=nothing priority:H status:pending"
# when = "which task"
# symbol = " "
# style = "bold red"
# format = "[$symbol$output]($style)"
# description = "Count of pending Taskwarrior tasks"

# [custom.low]
# command = "task count rc.gc=off rc.verbose=nothing priority:L status:pending"
# when = "which task"
# symbol = "Q "
# style = "bold purple"
# format = "[$symbol$output]($style)"
# description = "Count of pending Taskwarrior tasks"

[line_break]
disabled = true


#Disable the package module, hiding it from the prompt completely
[package]
disabled = true


# [git_status]
# conflicted = "🏳"
# ahead = "🏎💨"
# behind = "😰"
# diverged = "😵"
# up_to_date = "✓"
# untracked = "🤷"
# stashed = "📦"
# modified = "📝"
# staged = '[++\($count\)](green)'
# renamed = "👅"
# deleted = "🗑"


[character]
success_symbol = "[❯](bold green) "
error_symbol = '[✗](bold red) '
vicmd_symbol = '[N](bold cyan) '
vimcmd_replace_one_symbol = '[❮](bold yellow)'
vimcmd_replace_symbol = '[❮](bold red)'
vimcmd_visual_symbol = '[V](bold purple)'


[hostname]
ssh_only = false
format = "[$hostname]($style) "
trim_at = "-"
style = "bold blue"
disabled = false

[python]
symbol = " "
style = "bold #e5c07b" #yellow

[nodejs]
symbol = " "
style = "bold #e5c07b" #yellow

[golang]
symbol = " "

[directory]
truncate_to_repo = false
style = "bold cyan"
read_only = " "

[git_branch]
format = "[$symbol$branch]($style) "
symbol = " "

[time]
disabled = false
style = "bold purple"
format = '[\[$time\]]($style) '
time_format = "%r"
