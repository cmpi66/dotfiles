#!/bin/env bash

##FZF pdf script 

# IFS=$'\n' selected_pdfs=($(find "$HOME" -maxdepth 6 ! -path "$HOME/.config/*" -name "*.pdf" | fzf --multi --exit-0))

# if [ ${#selected_pdfs[@]} -gt 0 ]; then
#   for selected_pdf in "${selected_pdfs[@]}"; do
#     okular "$selected_pdf"
#   done
# fi

DMENU="dmenu -i -l 15 -p" 
# DMENU="rofi -dmenu -i -l 15 -p" 

pdfs=$(find "$HOME" -maxdepth 6 ! -path "$HOME/.config/*" -name "*.pdf" | $DMENU "Select PDF:")

if [ -n "$pdfs" ]; then
  okular "$pdfs"
fi


