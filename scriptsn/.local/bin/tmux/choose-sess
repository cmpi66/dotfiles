#!/usr/bin/env bash

# im thinking of creating a demenu prompt to select my most used sessions: zettle-ema, ja, exegol on laptop
# not sure if this is redundant since i have the sessionizer 't' command but thats slight slower, the above sessions are more involved.

export WM="hyprland"

case "$(printf " zettle\n jobapps\n pensess\n pythondev\n bashdev" | rofi -dmenu -p 'Sessions: ')" in
' zettle') attachzettle-kitty ;;
' jobapps') jobapps ;;
# ' pensess') pensess ;; # not relevant since this is itself, maybe i can move exegol to a vm on server? its jsut too slow compared to this one, or maybe jsut a regular session with exegol?
' pythondev') pythondev ;;
' bashdev') bashdev ;;
*) exit 0 ;;
esac

# jobapps
# zettle-ema-kitty
# pensess
